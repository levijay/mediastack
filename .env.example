# MediaStack v1.0.0 Environment Configuration
# =============================================================================

# =============================================================================
# REQUIRED - Application Security
# =============================================================================
# IMPORTANT: Generate a unique secret for production!
# Generate with: openssl rand -base64 32
JWT_SECRET=changeme_jwt_secret_minimum_32_characters_required

# =============================================================================
# PATHS - Adjust for your setup
# =============================================================================
# Configuration and database location
CONFIG_PATH=/mnt/user/appdata/mediastack

# Data path - contains media and downloads
# This gets mounted as /data inside the container
# Example structure:
#   /mnt/user/data/media/movies
#   /mnt/user/data/media/tv
#   /mnt/user/data/usenet
#   /mnt/user/data/torrents
DATA_PATH=/mnt/user/data

# =============================================================================
# USER AND PERMISSIONS (Unraid defaults)
# =============================================================================
PUID=99
PGID=100
UMASK=002
TZ=America/New_York

# =============================================================================
# NETWORK
# =============================================================================
# Web interface port
PORT=6767

# =============================================================================
# LOGGING
# =============================================================================
# Options: debug, info, warn, error
LOG_LEVEL=info

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Set to false to require login (recommended for exposed instances)
AUTH_DISABLED=true

# =============================================================================
# OPTIONAL - External Services
# =============================================================================
# TMDB API Key - Can also be configured in Settings UI
# Get your key from: https://www.themoviedb.org/settings/api
# TMDB_API_KEY=your_tmdb_api_key_here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secret: openssl rand -base64 32
# 3. Update JWT_SECRET with your generated secret
# 4. Adjust DATA_PATH for your system
# 5. Start MediaStack: docker-compose up -d
# 6. Access web UI: http://[YOUR-IP]:6767
# 7. Configure TMDB API key in Settings → General
# 8. Add root folders in Settings → Media Management (use /data/... paths)
# 9. Add indexers in Settings → Indexers
# 10. Add download clients in Settings → Download Clients

# =============================================================================
# DOCKER VOLUME MAPPING REFERENCE
# =============================================================================
# The docker-compose.yml maps these paths:
#   ${CONFIG_PATH}:/config     - Database and app config
#   ${DATA_PATH}:/data         - Media and downloads
#   ${BACKUP_PATH}:/backup     - Backup location
#
# Inside the container, use /data/... paths for root folders
# Example: /data/media/movies, /data/media/tv, /data/usenet
